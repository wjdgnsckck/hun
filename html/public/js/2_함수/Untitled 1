주제 : 로또구매 프로그램
요구사항
   1. 로또구매 버튼 클릭해서 실행
   ------------>버튼 클릭해서 함수 실행해야지
   2. 1~45개 버튼 화면에 표시 
   ----------->화면 표시구역 지정후 값넣기
   3. 6개 버튼 선택 ( 중복 불가능 ) / 구매취소
   -----------> 
   4. 6개 숫자 모두 선택시 추첨결과 버튼 활성화 / 유효성검사 
   5. 당첨번호 = 자동 난수(랜덤1~45) 6개
   6. 선택한 6개 숫자와 당첨번호6개와 비교해서 결과
   
   
 *  만약에 동일한 숫자를 입력했을때 해당 숫자를 취소
indexOf를 활용 splice를 이용 해당 인덱스 위치값 삭제
배열명.splice(배열명.indexOf(변수),삭제값);
   alert를 이용해서 취소합니다 알람출력.
   *현재 선택된 숫자들(배열상태)을 다른공간에 출력.
  document.querySelector('.선택번호출력구역').innerHTML = 선택번호목록;
   push와 splice를 사용시 작동해야 하니 위에 push와 splice에 
   선택번호출력()함수 넣기
*추첨결과 버튼을 클릭시 난수(1~45)사이 6개를 저장
난수 생성, 배열에 push(6개) 비교해야하니 배열하나더 생성
*추첨결과 와 선택번호목록과 비교해서 6개 동일1등 5개 2등 
			-단 선택번호가 6개 미만 일 경우에는 버튼 클릭시 차단
4개 3등 출력
추첨결과= 선택된 번호==추첨번호%6==0


배열비교
선택번호목록과 추첨번호목록
6개 같으면 1등
5개 같으면 2등
4개 같으면 3등
그외 없음
두배열의 인덱스 값 확인 후 6개 같으면 1등 출력..






